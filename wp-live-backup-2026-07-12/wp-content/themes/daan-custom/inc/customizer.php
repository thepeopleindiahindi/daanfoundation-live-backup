<?php
/**
 * Theme Customizer settings
 */
add_action( 'customize_register', 'daan_customize_register' );
function daan_customize_register( $wp_customize ) {
    // Logo handled by core custom-logo feature
    // Add custom sections in later phases
}
